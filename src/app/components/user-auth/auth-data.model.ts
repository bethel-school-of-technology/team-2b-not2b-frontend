export interface AuthData {
    firstName: string;
    lastName: string;
    email: string;
    username: string;
    password: string;
}

export interface LoginAuthData {
    email: string;
    password: string;
}